<header>
	<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{ path('home') }}">
				<img src="{{ asset('uploads/logo-next.png') }}" alt="Next.js Logo" height="40">
			</a>
			{% if app.user %}
				<div class="ms-auto">
					<span class=" pe-3">
						Bem-vindo,
						{{ app.user.userIdentifier }}
					</span>
					|
				</div>
			{% endif %}
			<div class="navbar-nav align-items-center">
				{% if not app.user %}
					<a href="{{ path('app_login') }}" class="nav-link">Login</a>
					|
				{% endif %}

				{% if app.user %}
					<a href="{{ path('usuarios') }}" class="nav-link">Usuários</a>
					|
				{% endif %}
				<a href="{{ path('usuarios_novo') }}" class="nav-link">Cadastrar</a>

				{% if app.user %}
					|
					<a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
				{% endif %}
			</div>
		</div>
	</nav>
</header>
